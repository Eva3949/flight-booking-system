version: '3.9'

services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: astu_flight
      MYSQL_USER: astu_user
      MYSQL_PASSWORD: astu_pass
